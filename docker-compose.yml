services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "true"
      DJANGO_ALLOWED_HOSTS: "*"
    # Live-reload the source for local dev; the image still runs standalone.
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
