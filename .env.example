# Copy to .env for local overrides. yfinance is keyless — no API keys here.
DJANGO_DEBUG=true
DJANGO_SECRET_KEY=dev-insecure-key-change-in-production
DJANGO_ALLOWED_HOSTS=*
# Comma-separated, required when serving behind Cloudflare/Render (https origins).
DJANGO_CSRF_TRUSTED_ORIGINS=
